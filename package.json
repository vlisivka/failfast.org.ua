{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "devDependencies": {
    "@snowpack/plugin-svelte": "^3.7.0",
    "mdsvex": "^0.9.8",
    "snowpack": "^3.3.7",
    "snowpack-plugin-mdsvex": "^0.2.2"
  },
  "dependencies": {
    "svelte": "^3.39.0"
  }
}
